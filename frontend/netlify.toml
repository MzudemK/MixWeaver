[build]
  command = "npm run build"
  publish = "dist"

# API Proxy configuration
[[redirects]]
  from = "/api/*"
  to = "https://mixweaver.onrender.com/api/:splat"
  status = 200
  force = true

# Handle React Router (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
